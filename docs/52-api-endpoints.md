# API Endpoint Discovery

This specification handles REST and GraphQL routing mappings found within the application, binding the frontend to backend dependencies.

## 1. Exchange Endpoints
- Base: `api.binance.com`
- Endpoints (Market Data, Trades, Account configuration)

## 2. Web3 Wallet Endpoints
- Base: Network/Chain RPCs
- Endpoints (Gas fees, DApp Discovery, Balances)
